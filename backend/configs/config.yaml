server:
  port: 8080
  mode: debug

database:
  host: localhost
  port: 3306
  username: root
  password: root123
  dbname: admin_system
  charset: utf8mb4

jwt:
  secret: admin-system-jwt-secret-key
  expire: 24
